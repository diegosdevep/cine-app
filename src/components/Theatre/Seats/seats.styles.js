import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  seats: {
    margin: 10,
    borderColor: 'grey',
    borderWidth: 0.5,
    borderRadius: 5,
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  textSeats: {
    color: 'black',
    fontWeight: 'bold',
    padding: 8,
  },
});
